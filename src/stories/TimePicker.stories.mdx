import TimePicker from "../components/P2TimePicker/P2TimePicker";
import {
  DocsContainer,
  Meta,
  Story,
  Canvas,
  ArgsTable,
} from "@storybook/addon-docs";

<Meta title="Forms/Time Picker" component={TimePicker} />

<h4 className="h4">Time Picker</h4>

export const Template = (args) => <TimePicker {...args} />;

<Canvas>
  <Story name="Base" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={TimePicker} story="Base" />
