import Container from "../components/P2Container";
import {
  DocsContainer,
  Meta,
  Story,
  Canvas,
  ArgsTable,
} from "@storybook/addon-docs";

<Meta title="Components/Container" component={Container} />

<h1>Container</h1>

<p>
  Rectangular box or element that holds other components such as text, images,
  icons or other types of content.
</p>
<h4>Usage</h4>
<ul>
  <li>Functions as a base of different component combinations</li>
  <li>Not to be used as a clickable component</li>
  <li>
    BG colour to use is determined by the type of components that will be placed
    inside
  </li>
</ul>

export const Template = (args) => (
  <Container {...args}>
    <div style={{ backgroundColor: "#E3F2FD", height: "60px" }}></div>
  </Container>
);

<Canvas>
  <Story name="Base" parameters={{ controls: { hideNoControlsWarning: true } }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Container} story="Base" />
