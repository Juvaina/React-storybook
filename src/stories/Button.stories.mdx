import Button from "../components/P2Button";
import ButtonGroup from "../components/P2ButtonGroup";
import { Meta, Story, Canvas, ArgsTable } from "@storybook/blocks";

<Meta title="Components/Button" component={Button} />

<h1>Button</h1>
<p>
  A button triggers an event or action. They let users know what will happen
  next.
</p>
<h4>Usage</h4>
<ul>
  <li>
    Use primary button (red) for information that needs to “shout” or be
    highlighted. Examples: Carousel banner, Important Notes on My HDBPage, and
    Nudges on My HDBPage
  </li>
  <li>
    Use primary button (teal) for action-driven CTAs such as starting an
    e-Service or initiate further reading
  </li>
  <li>Use secondary button as option for more subtle actions</li>
  <li>
    Use tertiary button for content cards that requires less prominent CTAs
  </li>
</ul>

export const Template1 = (args) => <Button {...args}>Button</Button>;

export const Template2 = (args) => <ButtonGroup {...args} />;

<Canvas>
  <Story
    name="Base"
    args={{
      variant: "primary-1",
      disabled: false,
    }}
  >
    {Template1.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Button} story="Base" />

<h3>Buttons with icon</h3>
<p>
  Visual approach for buttons to help reinforce an action that will be the
  result of clicking a button
</p>
<h4>Usage</h4>
<ul>
  <li>
    Use for CTA buttons that require user action, such as “Login”, "Choose
    files" or "Select from Map"
  </li>
  <li>
    Use Text and icon button for more subtle button requirements such as
    “download”, “delete” - Refer to use case
  </li>
</ul>

<Canvas>
  <Story
    name="Button with left icon"
    args={{
      variant: "primary-2",
      startIcon: <i className="bi bi-lock-fill pe-2"></i>,
    }}
  >
    {Template1.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Button} story="Button with left icon" />

<Canvas>
  <Story
    name="Button with right icon"
    args={{
      variant: "primary-1",
      endIcon: <i className="bi bi-paperclip ps-2"></i>,
    }}
  >
    {Template1.bind({})}
  </Story>
</Canvas>

<ArgsTable of={Button} story="Button with right icon" />

<Canvas>
  <Story name="Font Resizer">{Template2.bind({})}</Story>
</Canvas>

<ArgsTable of={ButtonGroup} story="Font Resizer" />
