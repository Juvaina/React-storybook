import Checkbox from "../components/P2Checkbox";
import {
  DocsContainer,
  Meta,
  Story,
  Canvas,
  ArgsTable,
} from "@storybook/addon-docs";

<Meta title="Forms/Checkbox" component={Checkbox} />

<h1>Checkbox</h1>

<p>
  Input control that allows a user to select one or more options from a number
  of choices.
</p>
<h4>Usage</h4>
<ul>
  <li>Use when users need to select multiple options from a list</li>
  <li>Use to get user's confirmation before proceeding to submit a form</li>
</ul>

export const Template = (args) => <Checkbox {...args} />;

<Canvas>
  <Story name="Base">{Template.bind({})}</Story>
</Canvas>

<ArgsTable of={Checkbox} story="Base" />
